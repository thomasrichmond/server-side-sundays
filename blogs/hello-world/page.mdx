---
title: "My first blog"
date: "2024-06-08"
summary: "First taste into MDX with RSCs"
---

# Hello World

```js
const [count, setCount] = useState(0);

return (
  <button
    onClick={() => {
      setCount(count + 1);
    }}>
    Count: {count}
  </button>
);
```

This is my blog

<Counter />
<CounterTwo />
